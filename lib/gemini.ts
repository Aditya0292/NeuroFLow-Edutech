export async function geminiGenerate(prompt: string) {
  // Backend stub: integrate Google Gemini SDK/API here.
  // Keep this pure and call it from route handlers.
  return {
    prompt,
    text: ""
  }
}

